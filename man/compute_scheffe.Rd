% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scheffe_test.R
\name{compute_scheffe}
\alias{compute_scheffe}
\title{Scheffe's Post-Hoc Mean Comparison and Contrast Evaluation Engine}
\usage{
compute_scheffe(
  data,
  trait,
  anova_results,
  total_replications,
  geno_col = NULL,
  alpha = 0.05,
  reporting_level = 2
)
}
\arguments{
\item{data}{A verified \code{data.frame} containing treatment lines/cultivars and the target phenotypic trait.}

\item{trait}{A single character string specifying the column name of the target trait.}

\item{anova_results}{A structured \code{list} derived from upstream ANOVA layouts containing an \code{anova_table}.}

\item{total_replications}{An integer specifying the absolute number of replication blocks (\eqn{r}).}

\item{geno_col}{A character string specifying the treatment/genotype column name. Defaults to auto-detecting \code{"Genotype"}.}

\item{alpha}{A numeric value defining the Type-I error rate ceiling threshold. Defaults to \code{0.05}.}

\item{reporting_level}{An integer vector flag defining console trace settings: \code{0} for silent execution, \code{1} for summary, and \code{2} for exhaustive tracking. Defaults to \code{2}.}
}
\value{
A structured named \code{list} of class \code{"list"} containing 4 computational components:
\item{scheffe_critical_value}{The absolute calculated scalar threshold value of Scheffe's adjustment.}
\item{sed}{The isolated Standard Error of Difference (SED).}
\item{comparison_matrix}{A detailed data frame containing pairwise differences, F-ratios, p-values, and significance status.}
\item{ranked_means}{A structured data frame containing sorted treatment means and significance group letters (\code{Scheffe_Letters}).}
}
\description{
The \code{compute_scheffe} function executes a rigorous, high-precision post-hoc mean separation
analysis using Scheffe's method. It evaluates critical differences, pairwise contrasts, and outputs
comprehensive ranking tables with statistical group letters.
}
\examples{
data(gv_data, package = "AgriDataTools")
reps <- length(unique(gv_data$Replication))
rcbd_results <- anova_rcbd(data = gv_data, trait = "PH", reporting_level = 0)

scheffe_output <- compute_scheffe(
  data = gv_data,
  trait = "PH",
  anova_results = rcbd_results,
  total_replications = reps
)
}
